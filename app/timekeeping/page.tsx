import Timekeeping from "./timekeeping";
import "./timekeeping.css";
export const metadata = {
  title: "Timekeeping — HDPM",
  robots: { index: false, follow: false },
};
export default function Page() {
  return <Timekeeping />;
}
