import Link from 'next/link';
import { requireRole } from '@/lib/require-role';
import { redirect } from 'next/navigation';
export default async function AdminPage(){const guard=await requireRole('admin');if(!guard.ok)redirect('/');return <main className="mx-auto max-w-4xl p-8"><h1 className="mb-6 text-3xl font-semibold">Administration</h1><Link href="/admin/user-settings" className="block rounded-xl border bg-white p-6"><h2 className="text-xl font-semibold">User settings</h2><p className="mt-2 text-sm">Turn app sections on or off for each person, and manage invoice, estimate, and template abilities. Review the change history.</p></Link><Link href="/admin/fee-management" className="mt-4 block rounded-xl border bg-white p-6"><h2 className="text-xl font-semibold">Fee Management</h2><p className="mt-2 text-sm">Fee Index by tier, and the owner-level fee increase campaign: targets, priorities, agreement dates, and status tracking.</p></Link></main>;}
