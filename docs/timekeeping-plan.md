# Timekeeping: agreed scope and implementation

September 15, 2026 · Built on `feature/timekeeping`. Craig applied the database migration; its tables, signature columns and command guard were verified. See [setup and pilot guide](timekeeping-setup.md) for enrollment and testing. Application deployment follows the merge to main.

## Confirmed requirements

- **Company → Timekeeping**, for each employee.
- **Twice monthly:** 1st–15th and 16th–last day. These exact dates supersede the earlier “every two weeks” answer.
- Personal scheduled weekdays and default start/end times autofill each sheet. Staff enter exceptions or use live clock in/out and paid/unpaid break controls.
- Work intervals, breaks, miles, vacation, sick, emergency work/phone flags, explicit no-work days, daily notes and pay-period notes.
- Admin-selected **Hourly / Salary** employee preset. Everyone records time. The payroll report identifies salary staff clearly.
- **Employee digital sign-off using their existing Microsoft company sign-in → assigned manager approval → Excel report.** The employee sign-off stores name, account email, database timestamp, certification wording and signed version. No client-supplied signer or timestamp is trusted.
- Employees see one active sheet. Admins see all retained employee detail, periods and saved report versions, including former staff. Assigned reviewers see their team's sheets.

## Workflow

1. Admin enrolls each staff-directory account, sets participation dates, payroll ID, pay basis and a separate approving manager.
2. Employee saves their usual weekdays, start/end and lunch window. Starting defaults: 7:00 AM–4:30 PM, noon–1:00 PM unpaid lunch, and two 10-minute paid rest breaks. Employees can stagger lunch times and edit actual daily times; LOA goes in comments. Initial blank sheets can apply these defaults using the refresh button; future sheets autofill automatically.
3. Staff review daily exceptions, leave, business miles and notes. Scheduled time remains separately labeled until confirmed. Paid breaks remain included in worked time; unpaid breaks are deducted.
4. At period-end, after all shifts have finished, employee checks the certification and clicks **Sign & submit to manager**. Server validation checks the complete day set, elapsed intervals and open clock.
5. Assigned manager reviews detail and either approves or returns with a reason. Nobody approves their own sheet, including an admin.
6. Admin creates a versioned Excel package only when every participating employee has a signed, manager-approved sheet. The package can be downloaded and emailed manually.
7. An admin can reopen a sheet with a reason. Corrections require employee sign-off and manager approval again. Prior signatures, approvals and edits remain in immutable audit events; previously generated packages retain their original snapshot.

## Calendar and access decisions

Pacific time governs dates. Missing periods are generated by a daily protected job and an on-open fallback. Uniqueness constraints make generation safe to retry.

The oldest unfinished draft/returned sheet remains an employee's one active sheet until submission; then the next unfinished/current sheet appears. A returned historical sheet becomes the active correction task, without granting an archive browser. Complete final sign-off and review before disabling a departing employee. Retained history stays available to admins after departure.

## Implemented technical boundaries

- One default start/end pair across selected weekdays; per-weekday variations are daily exceptions in this release.
- Manual intervals require valid start/end times before saving. Only the live clock can hold an unfinished shift. Overnight defaults and live punches split at midnight and period boundaries; manual overnight entry uses `24:00` on the first day and `00:00` on the next day.
- Leave entry asks for hours, suggesting the day's scheduled working hours when available. Partial leave requires adjusted work intervals.
- Saved exports contain Summary, Daily detail, Shifts and breaks, and Report notes. Daily category hours use four decimals; summary adds those daily figures. Exact timestamps are retained. No wages, overtime allocation, taxes, mileage reimbursement rates or leave accrual calculations.
- Digital sign-off uses the current Microsoft-authenticated application session. It does not launch a second Microsoft login or use a drawn signature. A returned/reopened sheet requires a new sign-off; an administrator cannot sign on another employee's behalf.
- All browser access uses authenticated server routes. RLS and table privileges deny direct anonymous/authenticated database access. The service-role RPC rechecks actors, ownership, review assignment and versions inside a transaction. Audit and export records are append-only.
- A single atomic command updates clock state and every affected period. Stale requests fail with a reload instruction instead of duplicating time or overwriting another device's changes.
- No external payroll integration, email sending, reminders, draft/exclusion exports, backup reviewer delegation, separate pay-date settings or workweek overtime report in this release.

## Verification

Automated tests cover dates/leap years, schedules, time zones/DST, overnight splits, breaks, leave and mileage totals, required days, overlap/future time rejection, scope enforcement, forged identity/signature input, stale updates, salary/notes/signatures in Excel and summary reconciliation.

The isolated PostgreSQL checks replay the migration and exercise employee/manager/admin permissions, signing, return/re-sign/approve, correction/export versioning, immutable history, private table/RPC access and clock versions.

The fictional local browser pilot exercised saved daily/period notes, mileage, salary display, employee sign-off, manager approval, rollover, payroll export generation and live clock/break/out. The Excel response was downloaded through the local test endpoint and parsed after Chrome blocked its automated download. A real Microsoft account pilot against a test Supabase environment remains a rollout check.
