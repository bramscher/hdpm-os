# HDPM-OS

Product name: **HDPM-OS** (GitHub remote `bramscher/hdpm-os` since 2026-08-03; local folder still `hdpm-chatbot`).

Maintenance OS build: read docs/maintenance-os/00-README.md before any maintenance-related work. Build one wave at a time (03-build-waves.md). The dashboard mockup (04) is the visual spec.

Current priorities: **docs/agent-os/10-restart-2026-08-20.md** — the restart plan. Read it before any agent-layer work. Loop 1 (Cheryl's estimate chase) is the only thing being built; everything else is behind the gate in that doc's §8. Architecture reference: docs/agent-os/00-DRAFT-master-plan.md. Prior priorities doc (historical): docs/maintenance-os/07-wave1-redirect.md. One brief per session, plan mode first.
