/**
 * Agent subpath barrel — identity & voice only (depends on the pure `actor`
 * convention, no DB/channel imports). Safe for a browser bundle via
 * `@habu/core/agent`.
 */
export * from './identity';
